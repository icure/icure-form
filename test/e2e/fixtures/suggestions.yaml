form: Suggestions
description: "Fixture for hierarchical suggestions: a dropdown fed by the harness options fixture, and text fields whose palette is fed by the harness suggestions fixture (Notes opts in through codifications, Flagged through `suggestions: true`, Free text does not opt in). See test/e2e/test-page/suggestion-fixtures.ts."
sections:
  - section: Coding
    fields:
      - field: Diagnosis
        type: dropdown
        span: 12
        codifications:
          - FIXTURE
      - field: Notes
        type: text-field
        span: 12
        schema: styled-text-with-codes
        codifications:
          - FIXTURE
      - field: Flagged
        type: text-field
        span: 12
        schema: styled-text-with-codes
        suggestions: true
        links: true
      - field: Free text
        type: text-field
        span: 12
        schema: styled-text-with-codes
