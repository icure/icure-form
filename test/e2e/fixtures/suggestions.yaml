form: Suggestions
description: Fixture for hierarchical suggestions — a dropdown fed by the harness options fixture and a text field whose palette is fed by the harness suggestions fixture (see test/e2e/test-page/suggestion-fixtures.ts)
sections:
  - section: Coding
    fields:
      - field: Diagnosis
        type: dropdown
        span: 12
        codifications:
          - FIXTURE
      - field: Notes
        type: text-field
        span: 12
        schema: styled-text-with-codes
        options:
          suggestions: FIXTURE
